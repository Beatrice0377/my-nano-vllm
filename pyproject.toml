[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "nano-vllm"
version = "0.2.0"
authors = [{ name = "Xingkai Yu" }]
license = "MIT"
license-files = ["LICENSE"]
readme = "README.md"
description = "a lightweight research inference engine derived from nano-vLLM"
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy>=1.26.0",
    "torch>=2.4.0",
    "triton>=3.3.1,<3.4",
    "transformers>=4.51.0",
    "flash-attn",
    "safetensors>=0.4.3",
    "tqdm>=4.66.0",
    "xxhash",
]

[project.urls]
Homepage="https://github.com/GeeeekExplorer/nano-vllm"

[tool.setuptools.packages.find]
where = ["."]
include = ["nanovllm*"]
